services:
  registry:
    image: ghcr.io/fluoritejs/registry:latest
    ports:
      - "3000:3000"
    volumes:
      - registry-data:/app/data
      - ./deployment.yaml:/app/deployment.yaml:ro
      - ./config.yaml:/app/config.yaml:ro
    restart: unless-stopped

volumes:
  registry-data:
